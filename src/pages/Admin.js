import React from 'react';
import AnimatedPage from '../components/AnimatedPage';

const Admin = () => (
  <AnimatedPage>
    <div style={{ textAlign: 'center', marginTop: '5rem' }}>
      <h2>Admin Dashboard</h2>
      <p>View all clients and their storage usage.</p>
    </div>
  </AnimatedPage>
);

export default Admin;